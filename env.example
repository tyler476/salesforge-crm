# ─────────────────────────────────────────────────────────────
# SalesForge CRM v2 — Environment Variables
# Copy this to .env and fill in your real values.
# NEVER commit .env to git.
# ─────────────────────────────────────────────────────────────

# ── Supabase (required) ───────────────────────────────────────
REACT_APP_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
REACT_APP_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ─────────────────────────────────────────────────────────────
# The following go in Supabase Edge Function Secrets:
# Supabase Dashboard → Edge Functions → Manage Secrets
# (NOT in your .env file — they're server-side only)
# ─────────────────────────────────────────────────────────────

# ── Resend (Email) ────────────────────────────────────────────
# Get from: resend.com → API Keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
FROM_EMAIL=crm@yourcompany.com     # Must be verified domain in Resend
FROM_NAME=Your Company Sales Team

# ── Twilio (SMS) ──────────────────────────────────────────────
# Get from: console.twilio.com → Account Info
# Your number must be registered in an A2P 10DLC campaign
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_FROM_NUMBER=+15551234567    # Your A2P-registered Twilio number (E.164)

# ── Retell AI (AI Calling) ────────────────────────────────────
# Get from: app.retellai.com → API Keys
RETELL_API_KEY=key_xxxxxxxxxxxxxxxxxxxxxxxx
RETELL_AGENT_ID=agent_xxxxxxxxxxxxxxxx    # Your agent ID from Retell dashboard
RETELL_FROM_NUMBER=+15551234567           # Your Twilio number as added in Retell
RETELL_WEBHOOK_SECRET=your_webhook_secret # Set this in Retell → Agent → Webhook

# ── Supabase Service Role (for webhook function only) ─────────
# Get from: Supabase → Settings → API → service_role key
# WARNING: This is a secret key — only use in edge functions, never in frontend
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
